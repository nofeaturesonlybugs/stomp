package stomp

// Signal is just the empty struct type; it exists to make our documentation prettier.
type Signal struct{}

// Sig is an instance of Signal and provided as a convenience.
var Sig Signal
